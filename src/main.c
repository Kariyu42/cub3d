#include "cub3d.h"
#include "err_type.h"

int	main(int ac, char **av)
{
	t_config	*config;

	if (ac != 2)
		return (ft_putendl_fd(ARG_ERR, -1));
	/*
	else if (parse_data() == FAILS || launch_game() == FAILS)
		return (-1);
	*/
	return (0);
}